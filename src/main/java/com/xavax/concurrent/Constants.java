package com.xavax.concurrent;

/**
 * Constants defines constant values used in this package.
 */
public class Constants {
  final static int BITS_PER_BYTE = Byte.SIZE; 
  final static int BITS_PER_INT  = Integer.SIZE;
  final static int BITS_PER_LONG = Long.SIZE;
  final static int LOG2_BITS_PER_BYTE = 3;
  final static int LOG2_BITS_PER_INT  = 5;
  final static int LOG2_BITS_PER_LONG = 6;
  final static int LOG2_MAX_SIZE = Long.SIZE - 1;
}
